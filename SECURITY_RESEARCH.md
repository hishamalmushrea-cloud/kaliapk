# SECURITY_RESEARCH.md
## Phase 0 — بحث الأمن (البند 50 و51 و52 و53)

| الحقل | القيمة |
|---|---|
| المرحلة | PHASE 0 |
| الحالة | بحث أولي · **مراجعة أمنية رسمية لم تتم بعد** |
| التاريخ | 2026-09-12 |
| المبدأ الحاكم (البند 51) | لا نستخدم Root إن لم يكن مطلوبًا · ولا نمنح صلاحيات تتجاوز الحاجة |

---

## 1. نموذج التهديد (Threat Model)

### 1.1 ما نحميه
1. **المستخدم** من تطبيقنا (أخطاء، صلاحيات زائدة، بيانات حساسة في السجلات).
2. **جهاز المستخدم** من بيئة Linux (أدوات Kali، حزم مُنزَّلة).
3. **بيانات المستخدم** داخل Linux home من التطبيقات الأخرى ومن النسخ الاحتياطية غير المؤمَّنة.
4. **التطبيق نفسه** من rootfs خبيثة أو حزم مخترقة.

### 1.2 ما لا نستطيع حمايته (يجب قوله بصراحة)
- ❌ لا يمكن توفير **عزل أمني حقيقي** بين "بيئة Linux" وبقية التطبيق في الوضع Rootless: PRoot لا يوفر namespaces ولا cgroups ولا عزل عمليات [1](https://github.com/termux/proot-distro).
- ❌ أي كود يعمل داخل rootfs يعمل بنفس UID ونفس نطاق SELinux الخاص بتطبيقنا → **بإمكانه قراءة/كتابة كل ما يصل إليه التطبيق**.
- ❌ لا يمكن منع أداة داخل Kali من استغلال صلاحيات التطبيق نفسه (الشبكة، التخزين).
- ⚠️ المستخدم غير root يظهر بصلاحيات root داخل PRoot [1](https://www.kali.org/docs/nethunter/nethunter-rootless/) — وهذا **وهم صلاحيات** قد يوحي بأمان غير موجود داخل الجلسة.

> 🔴 **أثر تصميمي مباشر:** يجب أن تُعامل بيئة Linux على أنها **نفس مستوى الثقة للتطبيق**، لا كصندوق رمل. أي وثيقة أو واجهة توحي بأنها "معزولة" هي ادعاء كاذب (البند 64).

---

## 2. حدود العزل (مطلوب في البند 24 و50)

| نوع العزل | متاح؟ | ملاحظة |
|---|---|---|
| Filesystem | ⚠️ ترجمة مسارات فقط | ليس عزلًا أمنيًا [1](https://github.com/termux/proot-distro) |
| Process | ❌ | نفس UID، شجرة عمليات مشتركة |
| Network | ❌ | شبكة Android نفسها |
| Kernel | ❌ | نواة واحدة مشتركة |
| SELinux boundary | ❌ | نفس النطاق (domain) للتطبيق |

---

## 3. SELinux
- منع `exec()` داخل بيانات التطبيق ناتج عن **سياسة SELinux** (W^X) [1](https://www.reddit.com/r/androiddev/comments/b2inbu/psa_android_q_blocks_executing_binaries_in_your/) → أي محاولة "تحايل" يجب أن تكون ضمن ما تسمح به السياسة (مثل التنفيذ عبر linker الموجود في `/system`) [1](https://github.com/termux/termux-exec-package)، **لا** عبر تعطيل SELinux.
- ❌ **مرفوض تمامًا:** أي حل يتطلب `setenforce 0`. هذا يضعف أمن الجهاز بالكامل.

---

## 4. أمن rootfs والملفات المُنزَّلة (البند 52)

| المتطلب | الحالة | ملاحظة |
|---|---|---|
| مصدر موثوق | ⚠️ **UNKNOWN** | الرابط المتداول لـ rootfs Kali مأخوذ من مستودع طرف ثالث [1](https://github.com/zulfi0/install_rootfs_android) — يجب الحصول على المصدر من نطاق رسمي |
| تحقق SHA256 | مطلوب | يجب نشر البصمة أو جلبها من المصدر الرسمي عبر HTTPS |
| تحقق الإصدار | مطلوب | تسجيل إصدار rootfs في بيانات وصفية |
| منع rootfs غير موثوق | مطلوب | رفض أي مصدر خارج قائمة مصادر موثوقة صريحة |
| تسجيل المصدر والإصدار | مطلوب | في سجل تشخيصي + واجهة "حول" |
| عدم تنفيذ ملفات غير موثوقة خارج البيئة | مطلوب | كل تنفيذ داخل rootfs محكوم بـ Runtime Manager |

### فحص المخاطر
| الخطر | الاحتمال | الأثر | التخفيف |
|---|---|---|---|
| rootfs معدَّلة/مخترقة | منخفض (إن التزمنا بالمصدر الرسمي) | **حرج** | HTTPS + SHA256 + (إن وُجد) توقيع GPG |
| هجوم رجل في الوسط أثناء التحميل | منخفض | عالٍ | HTTPS + تثبيت المصدر + تحقق البصمة |
| تلف أثناء التحميل | متوسط | متوسط | تحقق + إعادة محاولة + استئناف |
| حزمة apt خبيثة داخل Kali | متوسط | عالٍ | نفس سياسة Kali الرسمية؛ إبلاغ المستخدم أن المخاطر مسؤولية استخدامه لأدوات الأمن |
| مساحة تخزين غير كافية تؤدي لحالة غير متسقة | متوسط | متوسط | فحص مسبق + تحقق بعد الاستخراج + تنظيف |

---

## 5. الصلاحيات (البند 53)

كل إذن يجب أن يملك: الغرض · السبب · الاستخدام · الأثر الأمني.

| الإذن | الغرض | السبب | الاستخدام | الأثر الأمني | القرار المبدئي |
|---|---|---|---|---|---|
| `INTERNET` | وصول Linux للإنترنت + تحميل rootfs | جوهري | تنزيل الحزم/rootfs + شبكة Linux | يسمح لأي كود داخل rootfs بالاتصال بالشبكة | ✅ مطلوب |
| `FOREGROUND_SERVICE` | إبقاء الجلسة حيّة | جوهري | خدمة أمامية دائمة | إشعار دائم؛ أثر محدود | ✅ مطلوب |
| `POST_NOTIFICATIONS` | إشعار الخدمة الأمامية + التشخيص | نظامي | حالة Linux | محدود | ✅ مطلوب (مع شرح) |
| `FOREGROUND_SERVICE_SPECIAL_USE` (إن لزم) | نوع خدمة محدد | قد يطلبه النظام حسب النوع | يُقيَّم لاحقًا | — | ❓ UNKNOWN |
| `WAKE_LOCK` | منع النوم أثناء جلسة طويلة | اختياري | فقط أثناء الجلسة النشطة | استهلاك بطارية | ⚠️ اختياري — بحد أدنى |
| `READ/WRITE_EXTERNAL_STORAGE` | استيراد/تصدير ملفات المستخدم | حسب التصميم | تبادل ملفات | **واسع** | ⚠️ يُستبدل بـ SAF أو Media Store إن أمكن |
| `MANAGE_EXTERNAL_STORAGE` | وصول شامل للملفات | ❌ **مرفوض مبدئيًا** | — | **واسع جدًا** + قيود متجر | ❌ لا |
| `VIBRATE` / أخرى | غير ضرورية | — | — | — | ❌ لا |
| موقع/كاميرا/ميكروفون/جهات اتصال | ❌ غير مطلوبة للـ MVP | — | — | عالٍ | ❌ لا |

> ⚠️ **قاعدة:** أي إذن يُضاف لاحقًا يجب أن يُوثَّق في هذا الجدول أولًا بسببه وأثره، قبل إضافته للكود.

---

## 6. مخاطر الوضع Root (مستقبلي — البند 49)

| الخطر | ملاحظة |
|---|---|
| تصعيد صلاحيات | منح root لتطبيق واسع الصلاحيات = سطح هجوم ضخم |
| تسريب root إلى rootfs | يجب عزل مسار `su` ومنح الصلاحية عند الطلب فقط |
| استمرار الصلاحية | يجب ألا تبقى جلسة root مفتوحة بلا سبب |
| توافق الضمان/الأمان | إشعار واضح للمستخدم بالمخاطر |

---

## 7. النسخ الاحتياطي والسجلات (البند 50)

| البند | السياسة المقترحة | الحالة |
|---|---|---|
| النسخ الاحتياطي | يشمل ملفات المستخدم + home + الإعدادات + قائمة الحزم؛ **لا** يشمل حالة RAM | متوافق مع البند 38 |
| تشفير النسخة | يُقيَّم (ملفات المستخدم قد تحتوي بيانات حساسة) | ❓ UNKNOWN |
| السجلات | يجب **تنظيف** أي بيانات حساسة (مسارات، أسماء مستخدمين، مفاتيح) قبل العرض أو التصدير | مطلوب |
| التشخيص | يعرض "ماذا حدث / لماذا / كيف تُصلحه" بدون تسريب بيانات خاصة | البند 65 |

---

## 8. مهام أمنية مطلوبة قبل Phase 1

| # | المهمة | الأولوية |
|---|---|---|
| S-01 | تحديد المصدر الرسمي لـ rootfs Kali ARM64 + سياسة التحقق (SHA256/توقيع) | **عالية** |
| S-02 | نموذج تهديد مكتوب ومعتمد (هذا الملف نواة له) | عالية |
| S-03 | مراجعة صلاحيات نهائية مع مبرر لكل إذن | عالية |
| S-04 | سياسة السجلات والتشخيص (ما يُسجَّل وما يُخفى) | متوسطة |
| S-05 | سياسة النسخ الاحتياطي/التشفير | متوسطة |
| S-06 | مراجعة أمنية لآلية تجاوز W^X المختارة | **عالية** |
| S-07 | تقييم أمني لطبقة IPC بين Android وLinux | عالية |
| S-08 | مراجعة تراخيص كل مكوّن خارجي (مرتبط بـ LICENSES.md) | عالية |

---

## 9. ما لم يُبحث بعد
- تفاصيل نطاق SELinux الخاص بتطبيق يستهدف Android 15 على HyperOS.
- أثر `android:extractNativeLibs` على أمن تنفيذ binaries (C-01).
- أمان IPC بين التطبيق وخادم العرض (Termux:X11 أو بديلنا).
- أثر صلاحية التخزين على استيراد/تصدير الملفات.
